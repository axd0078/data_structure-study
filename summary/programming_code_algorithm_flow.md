# programming 代码算法流程总结

本文按 `programming` 目录的现有分组，逐题整理源码中的算法流程。每个文件都从三件事看：先把题目对象建模成什么数据结构，再看输入输出怎样接住，最后看算法步骤和容易写错的细节。

## 双指针与排序贪心

### `double-pointer-and-greedy/a+b+c=0.cpp`

- **思路 / 数据结构设计**：用 `int nums[]` 存所有数，先排序；用 `Node { int a, b, c; }` 保存每组三元组答案。固定第一个数 `nums[i]`，剩下两个数用 `left/right` 在有序数组中夹逼。
- **输入输出处理**：C 风格 `scanf` 读 `n` 和 `n` 个整数，答案存入动态分配的 `Node* arr`，最后逐行 `printf("%d %d %d\n", ...)`。
- **算法细节问题**：排序后如果 `nums[i] > 0` 可以提前结束；固定数和左右指针都要跳过重复值，避免输出重复三元组；`result < 0` 时左指针右移，`result > 0` 时右指针左移。时间复杂度 `O(n^2)`。

### `double-pointer-and-greedy/merge.cpp`

- **思路 / 数据结构设计**：用 `interval { int l, r; }` 表示区间，数组 `intervals[]` 保存全部区间，按左端点排序后从前往后合并。
- **输入输出处理**：`scanf` 读区间数量和每个区间端点；先输出合并后的区间个数，再逐行输出每个区间。
- **算法细节问题**：当前实现发现重叠后把后面的区间整体前移，属于就地删除，复杂度可能到 `O(n^2)`；复用时更推荐维护一个写入下标或答案数组。重叠判断要明确闭区间边界，常见写法是 `next.l <= cur.r`。

### `double-pointer-and-greedy/meeting.cpp`

- **思路 / 数据结构设计**：把会议拆成两个数组 `begin[]` 和 `end[]`，分别保存所有开始时间和结束时间。排序后用两个指针模拟时间线上会议开始和结束事件。
- **输入输出处理**：`scanf` 读 `n` 和 `n` 行 `[start, end]`，`printf` 输出最少会议室数量。
- **算法细节问题**：如果下一个开始时间小于最早结束时间，说明需要新增会议室；否则释放一个会议室。这里用 `begin[i] < end[j]`，表示同一时刻结束和开始可以共用会议室。复杂度 `O(n log n)`。

### `double-pointer-and-greedy/rain.cpp`

- **思路 / 数据结构设计**：用 `vector<int> height` 保存高度，左右指针分别站在数组两端，当前容量由较短边和宽度决定。
- **输入输出处理**：`cin` 读 `n` 和高度数组，`cout` 输出最大容量。
- **算法细节问题**：每次移动较短的一边，因为容量受短板限制，移动长边不会让当前宽度下的短板变高。数组长度小于 2 直接返回 0。复杂度 `O(n)`。

### `double-pointer-and-greedy/rain-plus.cpp`

- **思路 / 数据结构设计**：仍用高度数组和左右指针，但额外维护 `leftMax/rightMax`，表示当前位置两侧见过的最高柱子。
- **输入输出处理**：`cin` 读 `n` 和高度数组，`cout` 输出总接水量。
- **算法细节问题**：每次更新两侧最大值，移动较低一侧；如果左侧较低，左侧可接水为 `leftMax - height[left]`，右侧同理。长度小于 3 不可能接水。复杂度 `O(n)`。

## 二分

### `binary-search/find-mid.cpp`

- **思路 / 数据结构设计**：两个数组分别排序后，把总数组的左半部分切成 `i + j = k` 个元素。二分较短数组的切分点 `i`，另一个数组切分点由 `j = k - i` 决定。
- **输入输出处理**：`scanf` 读两个数组长度和元素，先 `qsort` 排序，再输出一位小数格式的中位数。
- **算法细节问题**：用 `INT_MIN/INT_MAX` 处理切在数组边界的情况；合法切分要求 `nums1Left <= nums2Right && nums2Left <= nums1Right`。总长度奇数取左半最大值，偶数取左半最大和右半最小的平均值。复杂度 `O(log min(m,n))`。

### `binary-search/lis-binary.cpp`

- **思路 / 数据结构设计**：`tails[i]` 表示长度为 `i + 1` 的递增子序列中最小的结尾值。每个新数用二分找第一个 `>= target` 的位置。
- **输入输出处理**：`scanf` 读 `n` 和数组，`printf` 输出 LIS 长度。
- **算法细节问题**：找到的位置如果等于当前长度，就扩展 `tails`；否则替换原位置，使同长度子序列的结尾尽量小。二分区间是 `[0, maxLen)`。复杂度 `O(n log n)`。

### `binary-search/minEatingSpeed.cpp`

- **思路 / 数据结构设计**：二分答案 `speed`。`getTime` 计算给定速度吃完所有香蕉需要多少小时。
- **输入输出处理**：`cin` 读 `pilesSize h` 和数组，用 `vector<int>` 接收，再把 `data()` 传给函数，`cout` 输出最小速度。
- **算法细节问题**：速度下界为 1，上界为最大堆香蕉数；`getTime` 中每堆耗时要向上取整。速度越大，耗时越小，满足单调性，可以二分最小可行值。复杂度 `O(n log M)`。

### `binary-search/shipWithDays.cpp`

- **思路 / 数据结构设计**：二分船的承载能力 `capacity`。`getDays` 顺序装货，超过容量就开启新一天。
- **输入输出处理**：`cin` 读货物数量、天数和每个重量，用 `vector<int>` 保存，输出最小运载能力。
- **算法细节问题**：容量下界是单件最大重量，上界是所有重量总和；容量越大，所需天数越少。`getDays` 初始天数为 1，遇到装不下时先 `day++` 再重置当天载重。复杂度 `O(n log S)`。

## 动态规划

### `dynamic-programming/coinChange.cpp`

- **思路 / 数据结构设计**：完全背包模型，`dp[x]` 表示凑出金额 `x` 需要的最少硬币数。`amount + 1` 作为不可达哨兵。
- **输入输出处理**：第一行用 `getline(cin >> ws, line)` 整行读取硬币列表，`parseCoins` 把非数字和非负号字符替换为空格，再用 `stringstream` 提取整数；第二行读 `amount`。
- **算法细节问题**：`dp[0] = 0`；枚举每个硬币后正序遍历金额，表示硬币可以重复使用；非正硬币跳过。最后如果仍为哨兵则输出 `-1`。复杂度 `O(amount * coinCount)`。

### `dynamic-programming/eraseOverlapIntervals.cpp`

- **思路 / 数据结构设计**：先把“删除最少区间”转成“保留最多互不重叠区间”。按右端点排序，`dp[i]` 表示前 `i` 个区间最多能保留多少个。
- **输入输出处理**：`cin` 读 `n` 和 `n` 个区间；对非法或空输入做提前返回；输出最少删除数。
- **算法细节问题**：`ends[]` 保存排序后的右端点，对当前区间起点 `start` 用 `upper_bound` 找前面有多少区间满足 `end <= start`。转移为 `dp[i] = max(dp[i-1], dp[j] + 1)`。复杂度 `O(n log n)`。

### `dynamic-programming/lcis.cpp`

- **思路 / 数据结构设计**：只需要两个变量，`pre` 表示以当前元素结尾的连续递增长度，`res` 表示答案。
- **输入输出处理**：`cin` 读 `n`，如果 `n == 0` 输出 0；之后读数组并输出 LCIS 长度。
- **算法细节问题**：当前数大于前一个数时 `pre++`，否则重置为 1。LCIS 要求连续，不能像 LIS 一样跳过元素。复杂度 `O(n)`。

### `dynamic-programming/lis.cpp`

- **思路 / 数据结构设计**：文件中保留两种做法。朴素 DP 用 `dp[i]` 表示以 `nums[i]` 结尾的 LIS 长度；优化版用 `dp[end]` 保存各长度的最小结尾值。
- **输入输出处理**：`cin` 读 `n` 和数组，当前 `main` 调用二分优化版并输出长度。
- **算法细节问题**：朴素版双层循环判断 `nums[j] < nums[i]`；二分版遇到更大值就扩展，否则找第一个 `>= nums[i]` 的位置替换。注意是严格递增，不能用 `>` 和 `>=` 混乱。复杂度分别为 `O(n^2)` 和 `O(n log n)`。

### `dynamic-programming/maxEnvelopes.cpp`

- **思路 / 数据结构设计**：二维信封先排序降维，再在高度上做 LIS。宽度升序，宽度相同时高度降序，防止同宽信封互相嵌套。
- **输入输出处理**：`cin` 读 `n` 和每个信封的宽高，空输入或 `n == 0` 直接输出 0。
- **算法细节问题**：排序后只比较第二维高度；源码中的 `dp` 存的是信封下标，不是直接存高度，所以比较时要访问 `envelopes[dp[mid]][1]`。复杂度 `O(n log n)`。

### `dynamic-programming/maxSubArray.cpp`

- **思路 / 数据结构设计**：Kadane 算法，`pre` 表示以当前元素结尾的最大子数组和，`res` 保存全局最大值。
- **输入输出处理**：`cin` 读 `n` 和数组，`n <= 0` 时直接结束。
- **算法细节问题**：转移为 `pre = max(nums[i], pre + nums[i])`，表示要么从当前数重新开始，要么接在前一段后面。全负数场景要用首元素初始化。复杂度 `O(n)`。

### `dynamic-programming/numSquare.cpp`

- **思路 / 数据结构设计**：`dp[i]` 表示组成整数 `i` 的最少完全平方数个数。
- **输入输出处理**：`cin` 读一个整数 `n`，输出 `dp[n]`。
- **算法细节问题**：`dp[0] = 0`；对每个 `i` 枚举 `j*j <= i` 的平方数，转移 `dp[i] = min(dp[i], dp[i - j*j] + 1)`。源码使用 `pow`，复用时可改成 `j * j` 减少浮点参与。复杂度 `O(n sqrt n)`。

## 图搜索

### `graph-search/dijkstra.cpp`

- **思路 / 数据结构设计**：`Graph` 结构体保存顶点数、边数和邻接矩阵 `edge[][]`；`distance[]` 保存起点到每个点的最短距离，`visited[]` 标记最短路已确定的点。
- **输入输出处理**：`scanf` 读顶点数、边数和每条边，矩阵先初始化为对角线 0、其余 `INF`；输出从 0 号点到最后一个点的距离，不可达输出 `-1`。
- **算法细节问题**：每轮从未访问点中找距离最小的点，再用它松弛所有邻接点。当前代码写入边时用了 `G.edge[-x][--y] = num`，说明它依赖特定题面下标或符号约定，移植时必须先统一成 0 基或 1 基下标。邻接矩阵版复杂度 `O(n^2)`。

### `graph-search/numOfLand.cpp`

- **思路 / 数据结构设计**：字符网格 `char** grid`，`'1'` 表示陆地，`'0'` 表示水。DFS 把访问过的陆地直接改成水。
- **输入输出处理**：`scanf` 读行列，逐行 `scanf("%s", grid[i])` 读字符矩阵，输出岛屿数量。
- **算法细节问题**：遍历每个格子，遇到 `'1'` 就答案加一并 DFS 淹没整个连通块。边界判断必须在访问数组前完成。复杂度 `O(row * col)`。

### `graph-search/numOfLand2.cpp`

- **思路 / 数据结构设计**：整数网格，`1` 表示陆地。先从边界出发 DFS，把所有连到边界的陆地清掉，剩下的才是封闭岛屿。
- **输入输出处理**：`scanf` 读行列和矩阵，动态分配二维 `int**`，输出封闭岛屿数量并释放内存。
- **算法细节问题**：要先处理第一行、最后一行、第一列、最后一列，再统计内部剩余连通块。DFS 访问后置 0 防止重复。复杂度 `O(row * col)`。

### `graph-search/sizeOfLand.cpp`

- **思路 / 数据结构设计**：和封闭岛屿类似，额外用全局变量 `size` 累加内部封闭陆地的总面积。`flag` 控制当前 DFS 是否计入面积。
- **输入输出处理**：`scanf` 读矩阵，输出面积总和。
- **算法细节问题**：边界连通陆地用 `flag = 0` 清掉但不计面积；内部陆地用 `flag = 1`，DFS 到一个格子就 `size++`。全局变量复用多组数据时需要手动清零。复杂度 `O(row * col)`。

### `graph-search/sonOfLand.cpp`

- **思路 / 数据结构设计**：两个整数网格 `grid1/grid2`。先把 `grid2` 中落在 `grid1` 水域上的连通块全部清掉，剩下的 `grid2` 岛屿就是子岛屿。
- **输入输出处理**：`scanf` 读行列，依次读两个矩阵，输出子岛屿数量并释放两张网格。
- **算法细节问题**：第一遍条件是 `grid1[i][j] == 0 && grid2[i][j] == 1`，这些岛不可能是子岛，要整块删除；第二遍统计 `grid2` 剩余连通块。复杂度 `O(row * col)`。

### `graph-search/numOfDiffLand.cpp`

- **思路 / 数据结构设计**：用 `vector<vector<int>> grid` 存网格，用 `path` 记录一个岛屿中每个格子相对起点的坐标，再把 `path` 放入 `set` 去重。
- **输入输出处理**：`cin` 读 `m n` 和整数网格，`cout` 输出不同岛屿形状数量。
- **算法细节问题**：相同形状出现在不同位置时，绝对坐标不同但相对坐标相同；DFS 方向顺序固定，路径表示才稳定。访问过的格子置 0。复杂度 `O(mn log K)`，`K` 为不同形状数。

## 贪心

### `greedy/arrayPairSum.cpp`

- **思路 / 数据结构设计**：排序后相邻两两配对，每对较小值就是贡献。用 `vector<int>` 保存数组。
- **输入输出处理**：`cin` 读 `n` 和数组，`cout` 输出最大贡献和。
- **算法细节问题**：升序后应取下标 `0, 2, 4...` 的元素；源码从 `i = 1` 开始累加 `min(nums[i], nums[i-1])`，效果等价。复杂度 `O(n log n)`。

### `greedy/candy.cpp`

- **思路 / 数据结构设计**：不用开糖果数组，维护上一人糖果数 `pre`、最近递增段长度 `inc`、递减段长度 `dec` 和总数 `ans`。
- **输入输出处理**：`cin` 读评分数组，输出最少糖果数。
- **算法细节问题**：评分上升时 `pre + 1`，相等时重置为 1；下降时递减段长度增加，如果 `dec == inc`，峰顶需要补一颗糖，所以 `dec++`。复杂度 `O(n)`。

### `greedy/eraseOverlapIntervals.cpp`

- **思路 / 数据结构设计**：区间按右端点升序排序，优先保留结束最早的区间，统计最多能保留多少个。
- **输入输出处理**：`cin` 读区间数量和端点，空输入输出 0。
- **算法细节问题**：当前区间起点 `>= end` 才能接在已选区间后面；最终答案是总数减去保留数。复杂度 `O(n log n)`。

### `greedy/findMinArrowShots.cpp`

- **思路 / 数据结构设计**：把每个气球看成区间，箭的位置 `pos` 放在当前可覆盖区间组的最小右端点。
- **输入输出处理**：`cin` 读 `n` 和每个气球左右边界，输出最少箭数。
- **算法细节问题**：按右端点排序；如果下一个气球左端点 `> pos`，当前箭射不到，需要新箭并更新 `pos`。边界相等时仍可被同一支箭射中。复杂度 `O(n log n)`。

### `greedy/increasingDigits.cpp`

- **思路 / 数据结构设计**：把数字转成 `string`，方便逐位修改。
- **输入输出处理**：`cin` 读整数，`cout` 输出不超过它的最大单调递增数字。
- **算法细节问题**：先找到第一处下降；再向左回退，遇到前一位仍大于后一位就前一位减 1；最后把修改位之后全部置为 `'9'`，保证结果最大。复杂度 `O(d)`。

### `greedy/jump.cpp`

- **思路 / 数据结构设计**：维护 `maxReached`，表示当前扫描到的位置能够覆盖到的最远下标。
- **输入输出处理**：`cin` 读数组，直接输出 `bool` 的 `0/1`。
- **算法细节问题**：如果扫描位置 `i > maxReached`，说明出现断点不可达；只要 `maxReached >= len - 1` 就可以提前返回 true。复杂度 `O(n)`。

### `greedy/jump2.cpp`

- **思路 / 数据结构设计**：把一次跳跃能覆盖的范围看成一层。`current` 是当前步数能覆盖的右边界，`next` 是再跳一步能覆盖的最远位置。
- **输入输出处理**：`cin` 读数组，输出最少跳数。
- **算法细节问题**：遍历到 `current` 时说明当前层结束，必须跳一次并把边界更新为 `next`；循环只到 `len - 2`，避免到终点后多加一步。复杂度 `O(n)`。

### `greedy/maxProfit.cpp`

- **思路 / 数据结构设计**：股票多次买卖可以把总利润拆成所有上升相邻差值之和。
- **输入输出处理**：`cin` 读价格数组，输出最大利润。
- **算法细节问题**：只要 `prices[i+1] > prices[i]` 就累加差值，相当于低点买入、高点卖出。复杂度 `O(n)`。

### `greedy/maxSubArray.cpp`

- **思路 / 数据结构设计**：用 `sum` 保存当前连续段贡献，`ans` 保存最大值。
- **输入输出处理**：`cin` 读数组，输出最大子数组和。
- **算法细节问题**：如果 `sum >= 0`，当前数接上前段更好；如果 `sum < 0`，从当前数重新开始。`ans` 用首元素初始化以支持全负数组。复杂度 `O(n)`。

### `greedy/meeting.cpp`

- **思路 / 数据结构设计**：按会议开始时间排序，用小根堆保存当前正在使用的会议室结束时间。
- **输入输出处理**：`cin` 读区间，`priority_queue<int, vector<int>, greater<int>>` 维护最早结束时间，输出最大堆大小。
- **算法细节问题**：处理新会议前，把所有 `end <= start` 的会议室弹出；再压入当前会议结束时间。堆的最大大小就是会议室数。复杂度 `O(n log n)`。

### `greedy/wiggleMaxLength.cpp`

- **思路 / 数据结构设计**：只记录上一个非零差值 `prev` 和当前差值 `curr`，遇到趋势反转就计数。
- **输入输出处理**：`cin` 读数组，输出最长摆动子序列长度。
- **算法细节问题**：相邻差为 0 不构成摆动；判断条件是 `(curr > 0 && prev <= 0) || (curr < 0 && prev >= 0)`。复杂度 `O(n)`。

## 字符串索引

### `string-index/countMatchingSubsequences.cpp`

- **思路 / 数据结构设计**：建立倒排索引 `index[26]`，每个字符保存它在主串中出现的所有下标。
- **输入输出处理**：`cin` 读主串、单词数量和每个单词，输出能匹配成子序列的单词数。
- **算法细节问题**：判断一个单词时维护当前位置 `p`，每个字符都用 `upper_bound` 找严格大于 `p` 的下一个出现位置；找不到就失败。适合多次查询同一主串。复杂度约为 `O(|s| + wordsTotalLen * log |s|)`。

## 数学与模拟

### `math-and-simulation/countPrimes.cpp`

- **思路 / 数据结构设计**：埃氏筛，用 `vector<bool> isprime(n, true)` 标记小于 `n` 的数是否可能为质数。
- **输入输出处理**：`cin` 读 `n`，输出小于 `n` 的质数个数。
- **算法细节问题**：从 `i = 2` 开始，只在 `isprime[i]` 为真时筛掉 `i*i, i*i+i...`；统计范围是 `[2, n)`，不包括 `n`。复杂度 `O(n log log n)`。

### `math-and-simulation/set-matrix-zeroes.cpp`

- **思路 / 数据结构设计**：动态二维数组 `int** nums` 保存矩阵，`row0[]` 和 `col0[]` 标记哪些行列需要置零。
- **输入输出处理**：`scanf` 读 `m n` 和矩阵，处理后按矩阵格式 `printf` 输出，并释放内存。
- **算法细节问题**：必须先标记再统一修改，不能边扫描边置零，否则新产生的 0 会污染后续判断。空间复杂度 `O(m+n)`。

## test 综合题

### `test/abbreviation-check.cpp`

- **思路 / 数据结构设计**：从原字符串中提取首字符和每个下划线后的字符，构造缩写 `temp`，再和目标字符串比较。
- **输入输出处理**：`scanf` 读测试组数和两个不含空格的字符串；结果先存入 `results[]`，最后统一逐行输出 `Yes/No`。
- **算法细节问题**：提取下划线后的字符时要从 `j = 1` 扫描，判断 `str1[j-1] == '_'`；`temp` 最后要补 `'\0'`。复杂度 `O(totalLen)`。

### `test/bestAttract.cpp`

- **思路 / 数据结构设计**：`Graph` 结构体包含距离矩阵 `matrix`、性别数组 `sex` 和人数 `size`。用 Floyd 求任意两人最短距离，再找异性对当前人的最大距离最小者。
- **输入输出处理**：`scanf` 读人数；每个人读 `sex size`，再读若干 `编号:距离`；输出女性候选编号一行、男性候选编号一行。
- **算法细节问题**：矩阵初始化时对角线为 0，其他为 `INF`；Floyd 三重循环前要跳过不可达中间状态，防止 `INF + INF` 干扰。评价候选人 `i` 时看所有异性 `j` 到 `i` 的距离最大值，最后分别按性别取最小最大值。复杂度 `O(n^3)`。

### `test/boss-time.cpp`

- **思路 / 数据结构设计**：`Time { int l, r; }` 把忙碌区间转换成秒数保存，排序后找相邻忙碌区间之间的空档。
- **输入输出处理**：使用 `scanf("%d:%d:%d - %d:%d:%d", ...)` 读取固定格式时间；`printTime` 用 `%02d` 输出补零时间。
- **算法细节问题**：先检查一天开始到第一个忙碌区间前是否空闲，再检查相邻区间，最后检查最后一个区间到当天结束。复用时要根据题面确认一天结束是否是 `23:59:59` 还是 `24:00:00`。复杂度 `O(n log n)`。

### `test/chihuoguo.cpp`

- **思路 / 数据结构设计**：按行扫描文本，用 `num` 记录总行数，`begin` 记录首次出现行，`ans` 记录关键词出现行数。
- **输入输出处理**：`getline` 读取整行，因为句子可能包含空格；读到单独的 `.` 结束。
- **算法细节问题**：用 `string::find` 判断是否包含 `chi1 huo3 guo1`，没找到时返回 `string::npos`。没有出现过输出 `-_-#`，否则输出首次行号和出现次数。

### `test/deliver_takeout.cpp`

- **思路 / 数据结构设计**：`tree[i]` 保存节点 `i` 的父节点，`visited` 标记已经走过的节点，`depth` 记忆化每个节点到根的深度。
- **输入输出处理**：`cin` 读节点数、订单数、父节点数组和每次配送地点；每处理一个地点输出当前最短总路程。
- **算法细节问题**：新增配送点时，`getDistance` 沿父链走到第一个已访问节点，只把新边数加入 `distance`；总往返距离是 `2 * distance`，但最后可以停在最深的订单点，所以减去 `maxDepth`。深度用路径回填避免重复计算。

### `test/greedy_multi_round_selection.cpp`

- **思路 / 数据结构设计**：`grade[i][0]` 是天梯赛分数，`grade[i][1]` 是 PAT 分数；`nums[score - 175]` 统计每个合格分数段的人数。
- **输入输出处理**：`cin` 读 `n k s` 和每个学生两项分数，输出最终推荐人数。
- **算法细节问题**：天梯赛分数低于 175 不考虑；PAT 分数达到阈值的学生直接计入且不占每段 `k` 个普通名额；其余每个天梯赛分数段最多取 `k` 人。排序在当前实现里不是最终计数所必需，但表达了按分数处理的思路。

### `test/guacaipiao.cpp`

- **思路 / 数据结构设计**：`map[3][3]` 保存九宫格，`visited[10]` 找出缺失数字，用 `choice` 决定求哪一行、列或对角线的和。
- **输入输出处理**：`scanf` 读九宫格、3 次查询坐标和最终选择编号；先输出 3 个查询位置的数字，再输出奖金。
- **算法细节问题**：格子里的 0 要按缺失数字参与求和；选择 1 到 3 表示行，4 到 6 表示列，7 和 8 表示两条对角线；最后用 `switch(point)` 映射奖金。

### `test/nursing-center.cpp`

- **思路 / 数据结构设计**：`unordered_map<string,int>` 把管理节点名映射为编号；`parent[u]` 保存管理节点父节点；`belong[x]` 保存老人 `x` 当前直属管理节点。
- **输入输出处理**：`cin` 读初始关系，`isNumber` 判断左侧是老人编号还是管理节点名；后续读操作字符 `Q/T/E`。
- **算法细节问题**：查询 `Q S` 时遍历所有老人，沿 `belong[i]` 的父链向上找是否经过节点 `S`，复杂度高但容易理解；转移 `T x S` 只改 `belong[x]`。当前代码的输出换行控制变量放在循环内，复用时应放到循环外。

### `test/nursing-center-plus.cpp`

- **思路 / 数据结构设计**：在朴素版基础上维护 `cnt[u]`，表示管理节点 `u` 的整棵子树中有多少老人。`addChain(u, val)` 沿父链给所有祖先加减计数。
- **输入输出处理**：使用 `ios::sync_with_stdio(false); cin.tie(nullptr);` 加速；读入方式和朴素版一致。
- **算法细节问题**：初始化时对每个老人所属节点执行 `addChain(..., +1)`；转移时旧链 `-1`，新链 `+1`；查询变成 `O(1)` 输出 `cnt[id]`。适合查询多、树高不太大的场景。

### `test/nursing-center-pro.cpp`

- **思路 / 数据结构设计**：把管理节点建成树，DFS 序把每个节点的子树映射成连续区间 `[in[u], out[u]]`；树状数组 `bit` 维护每个 DFS 位置上的老人数量。
- **输入输出处理**：`cin` 读关系和操作；`unordered_map` 编号管理节点；查询输出用 `first` 控制换行。
- **算法细节问题**：建树时 `tree[parent].push_back(child)`；找到 `parent[i] == 0` 的根后 DFS。老人属于节点 `u` 就在 `in[u]` 加 1；转移时旧位置减 1，新位置加 1；查询 `Q S` 是区间和 `sum(out[S]) - sum(in[S]-1)`。树状数组下标必须从 1 开始。

### `test/qinshidangan.cpp`

- **思路 / 数据结构设计**：把路径看成完全二叉树中的左右选择，`ans` 保存当前节点编号。
- **输入输出处理**：`scanf` 读路径长度 `N` 和条数 `M`，用 `getchar()` 吃掉换行，再用 `fgets` 逐行读取路径。
- **算法细节问题**：初始编号为 1；字符为 `y` 时走左孩子 `2 * ans - 1`，否则走右孩子 `2 * ans`。循环需要在 `N` 内并避开行尾换行。

### `test/random-fail.cpp`

- **思路 / 数据结构设计**：`k[]` 保存每一轮故意输之前要赢的次数，`index` 指向当前规则，`time` 记录当前规则下已经赢了几次。
- **输入输出处理**：`scanf` 读规则数量和数组，然后用 `fgets` 逐行读取对手手势，直到 `End`。
- **算法细节问题**：`win` 输出能赢对手的手势，`fail` 输出会输给对手的手势；当 `time == k[index]` 时故意输一次，然后 `time = 0` 且 `index` 循环后移。读行后要去掉末尾换行再比较。

### `test/safe-cell.cpp`

- **思路 / 数据结构设计**：`attackRow` 和 `attackCol` 标记已经被攻击过的行列，`row/col` 统计不同被攻击行列数量。
- **输入输出处理**：`cin` 读网格大小和 `q` 次操作，先把操作存入 `attack`，再统一处理，输出安全格子数。
- **算法细节问题**：同一行或列重复攻击只计一次；不安全格子数为 `row * m + col * n - row * col`，最后从总格子数扣除。总数要用 `long long` 防止乘法溢出。

### `test/stack-calculator.cpp`

- **思路 / 数据结构设计**：两个栈，`stack<long long> nums` 保存数字，`stack<char> ops` 保存运算符。因为题目按栈顺序计算，所以弹出时先得到右操作数。
- **输入输出处理**：`cin` 读 `n`、`n` 个数字和 `n - 1` 个运算符，输出最终结果或除零错误。
- **算法细节问题**：每次弹出 `n1` 和 `n2` 后执行 `n2 op n1`，顺序不能反；除法遇到 `n1 == 0` 输出 `ERROR: n2/0` 并结束。复杂度 `O(n)`。

### `test/stack_sequence_builder.cpp.cpp`

- **思路 / 数据结构设计**：`queue<int> conveyor` 表示传送带，`stack<int> box` 表示辅助盒子，`vector<int> tree` 表示当前松枝。
- **输入输出处理**：`cin` 读 `n m k` 和传送带序列；每构造完一条松枝就按空格分隔输出一行。
- **算法细节问题**：优先尝试盒子栈顶，因为盒子是后进先出；当前松枝必须保持非递增且长度不超过 `k`。如果传送带元素不能接入，就尝试放入盒子；盒子满或没有可用元素时输出当前松枝并重新开始。循环结束后如果 `tree` 中还有元素要补输出。

## test/csp 赛题

### `test/csp/1/1.cpp`

- **思路 / 数据结构设计**：用 `unordered_set<int>` 保存所有输入数字，再遍历集合中的正数，判断对应负数是否存在。
- **输入输出处理**：`cin` 读数字个数和每个数字，输出相反数对数量。
- **算法细节问题**：只统计 `x > 0` 的数字，避免 `x` 和 `-x` 被重复计算；集合自动去重，适合题目按不同数值统计相反数对的场景。复杂度 `O(n)`。

### `test/csp/1/2.cpp`

- **思路 / 数据结构设计**：`vector<Window>` 按从底到顶保存窗口，数组末尾代表当前顶层窗口。每次点击从后往前找第一个包含点击点的窗口。
- **输入输出处理**：`cin` 读窗口数量、点击次数、窗口坐标和每次点击坐标；命中输出窗口编号，否则输出 `IGNORED`。
- **算法细节问题**：窗口边界按闭区间判断；命中后先保存窗口，再从原位置删除并追加到数组末尾，表示该窗口被置顶。复杂度 `O(nm)`。

### `test/csp/2/1.cpp`

- **思路 / 数据结构设计**：用 `vector<int>` 保存全部输入数字，先整体排序，再只检查有序数组中相邻元素是否刚好相差 `1`。
- **输入输出处理**：`cin` 读取数字个数 `n` 和全部整数，最后输出满足条件的相邻数对数量。
- **算法细节问题**：题目只要求统计“差为 1”的数对数量，排序后这类数对一定出现在相邻位置；源码判断条件是 `nums[i] + 1 == nums[i + 1]`。复杂度 `O(n log n)`。

### `test/csp/3/1.cpp`

- **思路 / 数据结构设计**：用计数数组 `cnt` 记录每个编号已经出现了多少次，读到一个编号就先累加，再立刻输出当前计数。
- **输入输出处理**：`cin` 读取总次数 `n` 和后续编号序列，按输入顺序输出每次刷卡后的累计出现次数。
- **算法细节问题**：当前修改重点是首个输出项不补前导空格，后续项再统一前置空格；这类“边读边输”的题最容易错在格式控制。复杂度 `O(n)`。

### `test/csp/5/1.cpp`

- **思路 / 数据结构设计**：先单独读入第一个数字作为前驱 `pre`，之后只维护当前值 `x` 与前驱的比较结果；每次相邻值不同就说明进入了一个新的连续段。
- **输入输出处理**：`cin` 先读取整数数量 `n`，再读第一个值和剩余序列，最后输出连续变化段数量。
- **算法细节问题**：当前实现不是靠哨兵触发首段，而是先把第一个数读出来，再统计后续变化次数，最终输出 `ans + 1` 作为总段数。复杂度 `O(n)`。

### `test/csp/4/1.cpp`

- **思路 / 数据结构设计**：用二维 `vector<vector<int>>` 保存原矩阵，再按目标矩阵的行列顺序重新访问原矩阵完成逆时针旋转。
- **输入输出处理**：`cin` 读取原矩阵大小 `n、m` 和全部元素，输出时外层从最后一列往前枚举，内层从第一行到最后一行逐个打印。
- **算法细节问题**：输出矩阵大小会从 `n x m` 变成 `m x n`；每行内部要控制空格，源码通过 `if (j != 0)` 只在非首项前补空格。复杂度 `O(nm)`。

### `test/csp/6/1.cpp`

- **思路 / 数据结构设计**：把输入整数当作字符串 `num` 处理，逐字符累加每一位的数值，不需要反复做取模和整除。
- **输入输出处理**：`cin` 直接读取数字字符串，输出所有字符转数字后的总和。
- **算法细节问题**：`char` 转数字依赖 `ch - '0'`；字符串写法天然覆盖输入为 `0` 的场景，不需要额外特判。复杂度 `O(L)`，`L` 为数字位数。

### `test/csp/7/1.cpp`

- **思路 / 数据结构设计**：当前文件仍是空的程序骨架，还没有写入折点计数题对应的三点窗口或数组扫描逻辑。
- **输入输出处理**：仅保留了基础的快速输入输出设置，没有实际读入和输出代码。
- **算法细节问题**：后续补题时建议围绕连续三项比较 `(a[i-1], a[i], a[i+1])` 建模；目前文档只把它视为待补练习入口。

### `test/csp/8/1.cpp`

- **思路 / 数据结构设计**：当前文件同样只保留了空的 `main` 骨架，尚未写入最大波动题的相邻差值统计逻辑。
- **输入输出处理**：源码中仅启用了 `ios::sync_with_stdio(false)` 和 `cin.tie(nullptr)`，没有具体输入输出实现。
- **算法细节问题**：这类骨架文件适合后续直接在原路径补题；如果继续实现，核心会是在线维护相邻两天价格差的绝对值最大值。

### `test/csp/9/1.cpp`

- **思路 / 数据结构设计**：当前文件还只有空的 `main` 骨架，尚未写入题目建模和算法流程。
- **输入输出处理**：文件已启用 `ios::sync_with_stdio(false); cin.tie(nullptr);` 模板，后续可直接在此基础上补输入输出。
- **算法细节问题**：这类占位文件适合在真正补题时再同步补 README，避免文档把骨架误写成已完成实现。

### `test/csp/21/1.cpp`

- **思路 / 数据结构设计**：逐组读取安全得分 `s` 和权重 `w`，直接把 `s * w` 累加到答案中，不需要额外数组。
- **输入输出处理**：`cin` 读取记录数 `n`，再读取 `n` 组数据，最后输出安全指数。
- **算法细节问题**：乘积和建议用 `long long` 存储；全部累加结束后再与 `0` 取较大值，避免负答案直接输出。复杂度 `O(n)`。

### `test/csp/22/1.cpp`

- **思路 / 数据结构设计**：建立长度为 `L` 的计数数组 `cnt`，把整张灰度图中的每个像素值映射到对应桶中。
- **输入输出处理**：`cin` 读取行数 `n`、列数 `m` 和灰度范围 `L`，再读取 `n*m` 个像素，最后顺序输出每个灰度值出现次数。
- **算法细节问题**：读取时不需要保存整张图，直接做桶计数即可；输出阶段要控制最后一个数后不多打印空格。复杂度 `O(nm + L)`。

### `test/csp/22/2.cpp`

- **思路 / 数据结构设计**：用二维前缀和 `s[i][j]` 存矩阵左上角到当前位置的灰度总和，这样每个像素周围邻域矩形的和都能在 `O(1)` 内求出。
- **输入输出处理**：`cin` 读取矩阵大小 `n`、灰度上限 `L`、邻域半径 `r` 和阈值 `t`，再读取整张灰度图；最后输出满足邻域平均值不超过阈值的像素数量。
- **算法细节问题**：边界像素的邻域会被裁剪，面积不是固定值；判断平均值时直接比较 `sum <= t * area`，可以避免浮点除法。整体复杂度 `O(n^2)`。

### `test/csp/23/1.cpp`

- **思路 / 数据结构设计**：顺序扫描推导数组 `B`，一边把所有元素累加成总和上界，一边根据相邻关系维护最小可行总和。
- **输入输出处理**：`cin` 读取数组长度 `n` 和数组 `B`，最后按两行输出最大值和最小值。
- **算法细节问题**：当前实现把所有元素和记为 `max`，把满足 `B[i] > B[i-1]` 时需要补进来的值记到 `min`；变量名虽然与标准库同名，但作用域局部且不影响编译。复杂度 `O(n)`。

### `test/csp/24/1.cpp`

- **思路 / 数据结构设计**：把 `f(x)` 在每个区间 `[A[i], A[i+1])` 内恒等于 `i` 的性质直接拿来做分段贡献累计，不再枚举全部 `x`。
- **输入输出处理**：`cin` 读取分段点数量 `n`、上界 `N` 和递增数组 `A`，代码手动补上 `A[0] = 0` 与 `A[n+1] = N`，最后输出总和。
- **算法细节问题**：每段贡献就是 `i * (A[i+1] - A[i])`；`N` 可到较大范围，答案需要用 `long long` 保存。复杂度 `O(n)`。

### `test/csp/24/2.cpp`

- **思路 / 数据结构设计**：同时利用 `f(x)` 和 `g(x)` 都是分段常值函数的性质。外层按 `f(x)` 的区间扫描，内层再按 `g(x)` 的变化点把当前段切成更小的子区间。
- **输入输出处理**：`cin` 读取分段点数量 `n`、上界 `N` 和递增数组 `A`，补上两端边界后输出 `sum |g(x) - f(x)|` 的结果。
- **算法细节问题**：`r = N / (n + 1)` 后，`g(x)` 的下一个变化点是 `(g + 1) * r`；每次取 `min(right, (g + 1) * r)` 作为本段终点，就能保证误差值在该小段内恒定。复杂度可记为 `O(n)`。

### `test/csp/25/1.cpp`

- **思路 / 数据结构设计**：用布尔数组 `isInit` 标记变量是否已初始化，处理每条语句时先检查右值，再更新左值状态。
- **输入输出处理**：`cin` 读取变量数 `n` 和语句数 `k`，每条语句读取 `(op1, op2)`，最后输出告警次数。
- **算法细节问题**：编号 `0` 被视为已初始化，因此源码先设 `isInit[0] = true`；只要 `op2` 未初始化就计一次告警，然后再把 `op1` 标记为已初始化。复杂度 `O(k)`。

### `test/csp/27/1.cpp`

- **思路 / 数据结构设计**：用数组 `c[i]` 维护到第 `i` 位为止的累乘权值，把输入的整数 `m` 看成一组混合进制位在这些权值上的展开。
- **输入输出处理**：`cin` 读取位数 `n`、待展开整数 `m` 和每一位基数 `a[i]`，按顺序输出每一位的展开结果。
- **算法细节问题**：每一位答案都由“当前余量减去前面已还原贡献”再除以前一位权值得到，所以代码里用 `temp` 记录已经确定的低位贡献。复杂度 `O(n)`。

### `test/csp/28/1.cpp`

- **思路 / 数据结构设计**：把每一年对应的现金流看成一项折现值，直接按公式 `x / (1+i)^j` 逐项累计现值，不需要额外复杂结构。
- **输入输出处理**：`scanf` 读取年数 `n`、利率 `i` 和 `n+1` 个现金流，最后输出浮点结果。
- **算法细节问题**：指数是负的年份偏移，所以代码里用 `pow(1 + i, -j)`；这类题的关键不在循环本身，而在公式代入和浮点输出格式。复杂度 `O(n)`。

### `test/csp/28/2.cpp`

- **思路 / 数据结构设计**：每门课程至多依赖一个编号更小的前置课程，因此可以先顺推最早开始时间 `earliest[i]`，再在整体可行时逆序回推最晚开始时间 `latest[i]`。
- **输入输出处理**：`cin` 读取总天数 `n`、课程数 `m`，以及依赖数组 `p[]` 和耗时数组 `t[]`；先输出所有课程的最早开始时间，若计划可行，再输出最晚开始时间。
- **算法细节问题**：最早结束时间判定是 `earliest[i] + t[i] - 1`；逆推阶段从后往前扫描，把子课程的最晚开始约束回传给父课程，即 `latest[p[i]] = min(latest[p[i]], latest[i] - t[p[i]])`。复杂度 `O(m)`。

### `test/csp/29/1.cpp`

- **思路 / 数据结构设计**：把每个小矩形和目标大矩形 `[0, a] x [0, b]` 做一次裁剪，裁剪后剩余的宽和高相乘就是有效覆盖面积。
- **输入输出处理**：`cin` 读取矩形数量 `n` 和目标矩形尺寸 `a、b`，再读取每个子矩形的左下和右上坐标，输出累计面积。
- **算法细节问题**：交集宽度和高度分别用 `max(0, min(右边界) - max(左边界))` 计算，没有交集时自然回到 `0`，避免出现负面积。复杂度 `O(n)`。

### `test/csp/29/2.cpp`

- **思路 / 数据结构设计**：把每个任务记成 `(t, c)`，按目标时间 `t` 排序后，对答案“最终压到哪一天”做二分；`getNeed(day)` 负责计算把所有截止时间大于 `day` 的任务压缩到该天需要多少总代价。
- **输入输出处理**：`cin` 读取任务数 `n`、预算 `m`、允许的下界 `k` 以及每个任务的时间和单位代价，最后输出最小可行目标天数。
- **算法细节问题**：可行性函数对 `day` 单调，所以可以二分；排序后用 `upper_bound` 直接跳过已经不需要压缩的前缀，减少判定时的无效扫描。复杂度约为 `O(n log n + n log T)`。

### `test/csp/30/1.cpp`

- **思路 / 数据结构设计**：把每个 `8 x 8` 棋盘按读入顺序拼成一个长度固定的状态串，用 `map<string, int>` 维护每种状态已经出现过多少次。
- **输入输出处理**：`cin` 先读取棋盘数量 `n`，之后每个棋盘连续读取 8 行字符串；每读完一个棋盘就输出该状态当前的累计出现次数。
- **算法细节问题**：棋盘大小固定，所以关键不是单次编码成本，而是保证同一棋盘总能转成完全一致的键值；代码中直接使用 `++map[state]` 同时完成自增和输出。复杂度约为 `O(n log K)`，`K` 为不同状态数。

### `test/csp/31/1.cpp`

- **思路 / 数据结构设计**：所有操作都只是平移，因此只需维护总位移向量 `(dx, dy)`，不需要保存完整操作序列。
- **输入输出处理**：`cin` 读取平移操作数 `n`、查询数 `m`，先读完 `n` 组平移量，再对每个查询点 `(x, y)` 输出平移后的新坐标。
- **算法细节问题**：总位移量可能累加很多次，源码用 `long long` 保存 `dx` 和 `dy`；预处理结束后每个查询只需要做一次加法，整体复杂度 `O(n + m)`。

### `test/csp/31/2.cpp`

- **思路 / 数据结构设计**：把所有操作拆成两类前缀信息：`scale[i]` 记录前 `i` 个操作的缩放积，`angle[i]` 记录前 `i` 个操作的总旋转角度。
- **输入输出处理**：`cin` 读取操作数 `n`、查询数 `m` 和每个操作，再对每个区间查询读取 `left/right/x/y`，输出变换后的点坐标。
- **算法细节问题**：区间 `[left, right]` 的实际变换只需要用前缀比值 `scale[right] / scale[left-1]` 和前缀角度差 `angle[right] - angle[left-1]` 恢复，不必逐条重放操作。复杂度 `O(n + m)`。

### `test/csp/33/1.cpp`

- **思路 / 数据结构设计**：`belong[s]` 维护编号 `s` 出现过哪些集合编号，`cnt[s]` 维护编号 `s` 的总出现次数，两套统计分别对应“不同归属数”和“总次数”。
- **输入输出处理**：`cin` 读取集合数量 `n`、编号范围 `m`，再读取每个集合内的元素列表；最后对 `1..m` 顺序输出两项统计结果。
- **算法细节问题**：用 `unordered_set` 去重后，`belong[i].size()` 就是某编号出现于多少个不同集合；`cnt[i]` 则无论是否重复都累加。复杂度与输入元素总数线性相关。

### `test/csp/33/2.cpp`

- **思路 / 数据结构设计**：用两个 `set<string>` 分别保存两组单词，插入前统一转成小写，保证大小写不同但含义相同的词会合并到同一元素。
- **输入输出处理**：`cin` 读取两组单词数量和具体字符串，先输出交集大小，再输出并集大小。
- **算法细节问题**：转小写要在插入集合前完成，否则集合去重会失效；后续直接用 `set_intersection` 和 `set_union` 统计结果，逻辑比手写计数更稳定。复杂度约为 `O((n+m) log(n+m))`。

### `test/csp/34/1.cpp`

- **思路 / 数据结构设计**：先把原矩阵按行优先顺序压进一维数组，再把这段连续数据按新的 `p x q` 规格重新输出。
- **输入输出处理**：`cin` 读取原矩阵大小 `n`、`m` 与目标大小 `p`、`q`，随后读取 `n*m` 个元素；输出阶段按 `p` 行、每行 `q` 个数顺序打印。
- **算法细节问题**：当前实现不做复杂坐标换算，只维护一个线性下标 `index` 顺次取值；这类题最容易错在行末空格与换行控制。复杂度 `O(nm + pq)`。

### `test/csp/34/2.cpp`

- **思路 / 数据结构设计**：矩阵主体保存在一维数组 `matrix` 中，`n` 和 `m` 维护当前行列数；不同操作分别对应改维度、做一次完整转置、或按当前位置直接查询元素。
- **输入输出处理**：`cin` 读取初始矩阵大小、操作数 `t` 和全部矩阵元素，之后每次读取 `(op, a, b)` 执行操作；查询操作即时输出答案。
- **算法细节问题**：转置时要新建临时数组，并按 `temp[j * n + i] = matrix[i * m + j]` 重映射，再交换 `n` 和 `m`；如果忘记同步新维度，后续查询下标都会错位。复杂度与转置次数相关，最坏可写成 `O(nm + tnm)`。

### `test/csp/34/3.cpp`

- **思路 / 数据结构设计**：当前文件只保留了标准 C++ 程序骨架，还没有具体题意对应的数据结构设计。
- **输入输出处理**：源码中仅保留 `ios::sync_with_stdio(false)` 和 `cin.tie(nullptr)`，尚未接入实际输入输出。
- **算法细节问题**：这类占位文件在后续补题时，需要先确定题目模型，再决定是走模拟、图论还是字符串处理路线；目前不应把它视为已完成实现。

### `test/csp/35/1.cpp`

- **思路 / 数据结构设计**：顺序扫描字符串，分别维护“是否出现字母 / 数字 / 特殊字符”三类标记，再用字符频次表判断是否有字符出现至少三次。
- **输入输出处理**：`cin` 读取测试组数 `n`，之后逐个读取密码串并输出分类结果 `0 / 1 / 2`。
- **算法细节问题**：当前实现是在某字符第三次出现时把重复条件置真，所以检查写在 `map[ch]++` 之前；最终结果还要和三类字符是否齐全联合判断。复杂度 `O(L)`。

### `test/csp/35/2.cpp`

- **思路 / 数据结构设计**：把字符替换规则看成一个定义在固定字符集上的函数映射，`up[bit][c]` 表示字符 `c` 经过 `2^bit` 次变换后的结果。这样多轮字符串变换就能转成函数复合的倍增跳转。
- **输入输出处理**：先读带包围符号的原始字符串，再读单次字符替换规则数量 `n` 和每条映射，最后对每个查询读取轮数 `k` 并输出变换后的整串，结果仍用 `#...#` 包起来。
- **算法细节问题**：初始化时要把字符集中的每个字符都映射到自己，再只覆盖题目给出的替换边；查询阶段对字符串中的每个字符按 `k` 的二进制位逐层跳转，避免真的循环执行 `k` 轮。复杂度可按 `O(n + qL log k)` 理解。

### `test/csp/36/1.cpp`

- **思路 / 数据结构设计**：把棋盘位置看成一个可更新的 `(x, y)` 状态，逐字符读取操作串，针对四个方向分别尝试移动一步。
- **输入输出处理**：`cin` 读取棋盘大小 `n`、询问数 `k`，每组读取起点和操作串，输出最终停留坐标。
- **算法细节问题**：辅助函数 `inGraph` 负责边界判断，只有目标位置仍在 `1..n` 内时才真正更新坐标；每条指令最多处理一次，复杂度取决于操作串总长度。

### `test/csp/36/2.cpp`

- **思路 / 数据结构设计**：先构造前缀和 `S[i]`，再分别维护 `pre_max[i]` 和 `suf_max[i]`，把每个位置的答案拆成“左边历史最大值”和“右边延伸后的最大值”两部分比较。
- **输入输出处理**：`cin` 读取 `n`、数组 `a[0..n]` 和 `b[1..n]`，对每个 `i` 输出一项独立答案。
- **算法细节问题**：当前文件原先缺了一个右花括号，这次已经补齐；实现上先预处理三组数组，再在一次循环里按 `max(pre_max[i-1], suf_max[i] + b[i])` 输出结果，复杂度 `O(n)`。

### `test/csp/36/4.cpp`

- **思路 / 数据结构设计**：把“用 `steps` 步能到达的所有位置”看成一层区间 `[scanLeft, curRight]`，扫描这一层时不断更新下一层最远可达右端点 `nextRight`。
- **输入输出处理**：`cin` 读取数组长度 `n` 以及 `a[]`、`k[]` 两组数据，最后输出最少步数；若某一层无法继续扩展则输出 `-1`。
- **算法细节问题**：`scanLeft` 只从上一轮新加入的左端开始，避免重复扫描旧位置；当 `nextRight == curRight` 时说明进入死局，需要立即结束。整体是按层推进的线性扫描，复杂度 `O(n)`。

### `test/csp/37/1.cpp`

- **思路 / 数据结构设计**：直接枚举区间 `[l, r]` 内的偶数点，把每个点代入二次式 `x^2 + bx + c`，累加后统一乘以 2。
- **输入输出处理**：`cin` 读系数 `b c` 和区间 `l r`，输出求和结果。
- **算法细节问题**：从第一个不小于 `l` 的偶数开始按步长 2 枚举；乘法和累加使用 `long long` 防止溢出。复杂度约为 `O((r-l)/2)`。

### `test/csp/37/3.cpp`

- **思路 / 数据结构设计**：当前文件仍处于占位状态，没有进入题意建模阶段，因此也没有确定的数据结构。
- **输入输出处理**：源码只保留了基础的快速输入输出设置，没有实际读入和输出逻辑。
- **算法细节问题**：后续补题时建议先明确模板展开问题里的状态表示、占位符替换顺序和边界输入，再决定使用字符串扫描、语法树还是映射表。

### `test/csp/38/1.cpp`

- **思路 / 数据结构设计**：把输入值 `n` 在线性区间 `[a, a+b]` 中的位置换算成百分比，再把两位十进制百分数拆成行列坐标。
- **输入输出处理**：`cin` 先读测试组数 `k`，每组读取 `a b n`，逐组输出 1-based 的网格行列编号。
- **算法细节问题**：当前实现先算 `(n-a)/b * 100`，再截断成整数；十位作为行、个位作为列，最终输出时都要加 `1`。复杂度 `O(k)`。

### `test/csp/38/2.cpp`

- **思路 / 数据结构设计**：把棋盘格子看成图上的节点，从起点按 8 个马步方向做 BFS，队列中的状态额外记录当前已经走了多少步。
- **输入输出处理**：`cin` 读取棋盘大小 `n`、最大步数 `k` 和起点坐标，最后输出不超过 `k` 步可达的不同格子数量。
- **算法细节问题**：`visited` 在入队前标记，保证同一格子只会被统计一次；当状态步数已经达到 `k` 时不再继续扩展。最坏情况下会访问整个棋盘，复杂度 `O(n^2)`。

### `test/csp/38/3.cpp`

- **思路 / 数据结构设计**：把 72 位消息拆成固定字段处理，普通代码用 38 进制编码，典型代码用混合进制编码；无法直接逆推的摘要字段通过历史哈希表补全。
- **输入输出处理**：`cin` 先读消息条数，再逐条读取 72 位二进制串；按首位区分简单消息和复杂消息，输出接收方、发送方以及可选位置字段。
- **算法细节问题**：`getBits` 统一负责按位切片，`decodeNormal` 和 `decodeTypical` 分别处理两类代码；输出当前消息后，才把本条消息里直接出现的代码加入 `hash12` / `hash25`，这样才能符合“只依赖历史记录”的题意。单条消息处理长度固定，整体复杂度 `O(n)`。

### `test/csp/39/1.cpp`

- **思路 / 数据结构设计**：统计随机点中落在半径为 `a` 的圆内的比例，再用面积比例估算圆周率。
- **输入输出处理**：`cin` 读样本点数量和半径，再读每个点坐标；输出固定 6 位小数的 `pi` 估算值。
- **算法细节问题**：点在圆内的判断条件是 `x*x + y*y <= a*a`；最终公式是 `4.0 * m / n`。复杂度 `O(n)`。

### `test/csp/39/2.cpp`

- **思路 / 数据结构设计**：对每个可能的 5×9 子窗口计算“黑色最大值”和“白色最小值”，从而得到合法阈值区间，再用差分数组记录所有窗口的区间覆盖。
- **输入输出处理**：`cin` 读矩阵大小 `n`、灰度上限 `L` 和矩阵内容；最后逐行输出所有可行阈值。
- **算法细节问题**：模板大小固定，所以每个窗口内部扫描可视为常数；区间 `[left, right]` 用差分 `diff[left]++`、`diff[right+1]--` 记录，最后前缀和恢复可行阈值。复杂度 `O(n^2 + L)`。

### `test/csp/39/3.cpp`

- **思路 / 数据结构设计**：当前文件仍是空骨架，还没有把 HTTP 头信息题拆成键值对、请求头序列或状态机等具体结构。
- **输入输出处理**：现阶段没有实际的读入解析和结果输出代码，仅保留程序入口。
- **算法细节问题**：如果后续继续实现，这类题通常要先明确头字段的解析规则、大小写处理和多行输入边界；在此之前，文档仅把它标记为待补文件。

### `test/csp/40/1.cpp`

- **思路 / 数据结构设计**：标准集合 `S_i` 保留原下标序列，同时计算对应元素异或值；待校验集合 `T_i` 做相同处理，再比较两种信息是否一致。
- **输入输出处理**：`cin` 读原数组、标准集合分组和待校验集合分组；每组输出 `correct` 或 `wrong`。
- **算法细节问题**：源码用 `(s[i] == t) == (xorT == xorS[i])` 判断两者一致性，强调“顺序内容”和“异或摘要”应该同时匹配或同时不匹配。复杂度取决于所有集合元素总数。

### `test/csp/40/2.cpp`

- **思路 / 数据结构设计**：把 9 位状态拆成三段，`g(x, k)` 会按 `b -> c -> a` 的依赖顺序完成一次位运算映射。由于所有可能状态只有 `512` 个，所以先用数组 `ans[512]` 预处理每个状态经过全部参数后的结果。
- **输入输出处理**：`cin` 读输入个数 `n`、参数个数 `m` 和参数序列，再读每个待转换的数字；输出按序映射后的结果。
- **算法细节问题**：预处理阶段对每个初始状态都按参数序列逆序应用 `g`；`f` 和 `g` 内部都只在 3 位范围上做运算，最后再把三段拼回 9 位数。总体复杂度约为 `O(512m + n)`。

### `test/csp/40/3.cpp`

- **思路 / 数据结构设计**：矩阵本体存放在 `vector<string> g` 中，`rot` 记录当前整体旋转状态。访问单元格时先用 `pos(x, y)` 把逻辑坐标映射到真实存储坐标，再做读取或写回。
- **输入输出处理**：`cin` 读初始方阵、编码后的操作数组 `K`；把操作解析成 `Op` 后逆序执行，最后输出恢复后的有效矩阵尺寸和内容。
- **算法细节问题**：局部方阵旋转通过临时矩阵 `temp` 完成，恢复时要用逆旋转次数 `(4 - d / 90) % 4`；矩形翻转根据 `o` 判断是上下翻还是左右翻。整体旋转不直接改整张矩阵，而是只更新 `rot`，减少重复搬移。

### `test/csp/41/1.cpp`

- **思路 / 数据结构设计**：逐个把数字转成二进制，统计其中 `0` 位和 `1` 位的数量。
- **输入输出处理**：`cin` 读测试数字个数和每个数字，输出满足“二进制 0/1 个数相等”的数字数量。
- **算法细节问题**：当前实现通过 `a % 2` 判断末位是 0 还是 1，然后不断整除 2；复杂度为 `O(n log V)`。

### `test/csp/41/2.cpp`

- **思路 / 数据结构设计**：把任务分成可分割的 `flex` 和不可分割的 `normal` 两类；不可分割任务用 0/1 背包求咖啡减时，剩余咖啡再用分数贪心分配给可分割任务。
- **输入输出处理**：`cin` 读任务数量、咖啡总量和每个任务参数，输出最小总耗时，保留 6 位小数。
- **算法细节问题**：`normal` 部分先做一次背包，`flex` 部分按单位收益 `b/a` 排序后贪心分配；外层再枚举给 `normal` 使用的咖啡量，取全局最优。复杂度由咖啡上限和任务数共同决定。

### `test/csp/41/3.cpp`

- **思路 / 数据结构设计**：用 `map<long long, long long> freeByL` 按左端点维护空闲区间，用 `set<Segment, SegmentCmp> freeByLen` 按“长度优先、左端点次优先”维护可分配区间；每个进程保存若干 `QueueInfo` 记录当前队列的区间和下一次发送位置。
- **输入输出处理**：`cin` 读进程数和操作数；`new` 输出新分配区间的起点，`delete` 删除指定进程中的某个队列，`send` 输出本次所有队列发送位置之和。
- **算法细节问题**：分配时从 `freeByLen` 中找长度不小于 `L` 的最优区间，切出前缀并把剩余部分放回空闲集合；删除时要检查左右相邻空闲段并做区间合并；发送后 `next` 超过右端点要回绕到左端点，形成循环队列。主要操作复杂度为 `O(log n)`。

## test/gplt/2026 赛题

### `test/gplt/2026/all-things-i-can.cpp`

- **思路 / 数据结构设计**：用 `appeared[id]` 标记编号是否出现，用 `praised[id]` 标记是否被点赞，最后按编号升序筛选出现过但未被点赞的编号。
- **输入输出处理**：`cin` 读记录数量和每条 `编号 状态`；如果找到目标编号则空格分隔输出，否则输出 `NONE`。
- **算法细节问题**：编号范围固定到 `100000`，所以可以直接用布尔数组换取简单判断；输出时用 `first` 控制空格。复杂度 `O(n + U)`。

### `test/gplt/2026/check-homework.cpp`

- **思路 / 数据结构设计**：`current` 保存当前轮待检查编号，`left` 保存本轮未通过编号，`right` 保存通过编号。每轮处理完后用未通过组平均值更新阈值。
- **输入输出处理**：`cin` 读人数、初始阈值和每个编号的高度 / 分值；最终按进入 `right` 的顺序输出编号。
- **算法细节问题**：`current` 初始按 `n..1` 压入，再从末尾弹出以保持从小到大处理；进入下一轮前要复制 `left`、清空 `left` 并重置 `leftSum`。复杂度为 `O(n × 轮数)`。

### `test/gplt/2026/string-change.cpp`

- **思路 / 数据结构设计**：维护一个可变 `string s`，根据操作类型分别执行查找、插入和区间翻转。
- **输入输出处理**：`cin` 读操作数量和初始字符串；操作 `1` 输出子串前三个出现位置或 `-1`，操作 `2/3` 修改后输出当前字符串。
- **算法细节问题**：查找时从上一次命中位置 `pos + 1` 继续，避免重叠匹配被跳过；`reverse` 的右端点要使用 `r + 1`。复杂度约为 `O(qL)`。

### `test/gplt/2026/traffic-monitoring.cpp`

- **思路 / 数据结构设计**：用数组保存每个监测点流量，同时在线维护最大值、最小值和总和。
- **输入输出处理**：`cin` 读监测点数量和流量，先输出最大值、最小值、整数平均值，再输出超过平均值两倍的监测点编号或 `Normal`。
- **算法细节问题**：监测点编号按输入下标从 1 开始；异常条件是 `traffic[i] > ave * 2`，不是大于等于。复杂度 `O(n)`。
