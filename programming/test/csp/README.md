# CSP 练习题实现方法总结

## 模块简介

`programming/test/csp` 按 CSP 认证届次归档练习代码，当前目录既有已经完成的实现，也保留了少量后续待补的题目骨架。文档重点记录每道题使用的核心方法、实现切入点和编译说明，方便按届次复盘。

## 文件结构

| 目录 | 说明 |
|------|------|
| `1/` | 第 1 次认证题目 |
| `3/` | 第 3 次认证题目 |
| `5/` | 第 5 次认证题目 |
| `9/` | 第 9 次认证题目 |
| `21/` | 第 21 次认证题目 |
| `22/` | 第 22 次认证题目 |
| `23/` | 第 23 次认证题目 |
| `25/` | 第 25 次认证题目 |
| `27/` | 第 27 次认证题目 |
| `28/` | 第 28 次认证题目 |
| `29/` | 第 29 次认证题目 |
| `30/` | 第 30 次认证题目 |
| `31/` | 第 31 次认证题目 |
| `33/` | 第 33 次认证题目 |
| `34/` | 第 34 次认证题目 |
| `35/` | 第 35 次认证题目 |
| `36/` | 第 36 次认证题目 |
| `37/` | 第 37 次认证题目 |
| `38/` | 第 38 次认证题目 |
| `39/` | 第 39 次认证题目 |
| `40/` | 第 40 次认证题目 |
| `41/` | 第 41 次认证题目 |
| `README.md` | 当前目录说明 |

## 题目总览

| 文件 | 题目定位 | 核心方法 | 复杂度 |
|------|----------|----------|--------|
| `1/1.cpp` | 第 1 次 T1 | 哈希集合查找相反数 | `O(n)` |
| `1/2.cpp` | 第 1 次 T2 | 窗口层级模拟 | `O(nm)` |
| `3/1.cpp` | 第 3 次 T1 | 频次数组 + 即时输出 | `O(n)` |
| `5/1.cpp` | 第 5 次 T1 | 顺序扫描 + 相邻值比较 | `O(n)` |
| `9/1.cpp` | 第 9 次 T1 骨架 | 当前仅保留 `main` 模板 | `-` |
| `21/1.cpp` | 第 21 次 T1 | 加权求和 | `O(n)` |
| `22/1.cpp` | 第 22 次 T1 | 灰度直方图统计 | `O(nm + L)` |
| `23/1.cpp` | 第 23 次 T1 | 顺序扫描维护上下界 | `O(n)` |
| `25/1.cpp` | 第 25 次 T1 | 状态标记 + 条件计数 | `O(k)` |
| `27/1.cpp` | 第 27 次 T1 | 累乘权重 + 混合进制展开 | `O(n)` |
| `28/1.cpp` | 第 28 次 T1 | 折现公式累计 + 浮点幂运算 | `O(n)` |
| `29/1.cpp` | 第 29 次 T1 | 区间裁剪 + 面积累加 | `O(n)` |
| `29/2.cpp` | 第 29 次 T2 | 排序 + 二分可行目标天数 | `O(n log n + n log T)` |
| `30/1.cpp` | 第 30 次 T1 | 状态串压平 + `map` 计数 | `O(n log K)` |
| `31/1.cpp` | 第 31 次 T1 | 位移向量累加 + 查询直接平移 | `O(n + m)` |
| `31/2.cpp` | 第 31 次 T2 | 前缀缩放积 + 前缀旋转和 | `O(n + m)` |
| `33/1.cpp` | 第 33 次 T1 | `unordered_map` + `unordered_set` 计数 | `O(元素总数)` |
| `33/2.cpp` | 第 33 次 T2 | 集合去重 + 交并集统计 | `O((n+m) log(n+m))` |
| `34/1.cpp` | 第 34 次 T1 | 一维压平 + 顺序重构输出 | `O(nm + pq)` |
| `34/2.cpp` | 第 34 次 T2 | 矩阵转置重建 + 即时访问 | `O(nm + tnm)` |
| `35/1.cpp` | 第 35 次 T1 | 字符分类统计 + 频次计数 | `O(L)` |
| `36/1.cpp` | 第 36 次 T1 | 网格移动模拟 | `O(操作串总长)` |
| `36/2.cpp` | 第 36 次 T2 | 前缀和 + 前后缀最大值 | `O(n)` |
| `36/4.cpp` | 第 36 次 T4 | 分层区间推进 | `O(n)` |
| `37/1.cpp` | 第 37 次 T1 | 枚举偶数点代入求和 | `O((r-l)/2)` |
| `38/1.cpp` | 第 38 次 T1 | 百分比映射到网格 | `O(k)` |
| `38/2.cpp` | 第 38 次 T2 | BFS 统计马步可达格子 | `O(n^2)` |
| `38/3.cpp` | 第 38 次 T3 | 位段切分 + 历史哈希表 | `O(n)` |
| `39/1.cpp` | 第 39 次 T1 | 蒙特卡洛估算圆周率 | `O(n)` |
| `39/2.cpp` | 第 39 次 T2 | 模板扫描 + 差分统计 | `O(n^2 + L)` |
| `40/1.cpp` | 第 40 次 T1 | 集合异或校验 | `O(元素总数)` |
| `40/2.cpp` | 第 40 次 T2 | `512` 状态整表预处理 | `O(512m + n)` |
| `40/3.cpp` | 第 40 次 T3 | 逆序恢复 + 旋转映射 | `O(tZ^2)` |
| `41/1.cpp` | 第 41 次 T1 | 二进制位计数 | `O(n log V)` |
| `41/2.cpp` | 第 41 次 T2 | 0/1 背包 + 分数贪心 | `O(m×普通任务数 + m×灵活任务数 log 灵活任务数)` |
| `41/3.cpp` | 第 41 次 T3 | 空闲区间分配模拟 | `O(q log n)` |

## 核心实现说明

### 线性统计类

`3/1.cpp`、`5/1.cpp`、`21/1.cpp`、`22/1.cpp`、`25/1.cpp`、`30/1.cpp`、`33/1.cpp` 和 `35/1.cpp` 都是顺序扫描驱动的实现。重点不在复杂数据结构，而在于统计口径是否一致，例如 `3/1.cpp` 要边读边输出，`5/1.cpp` 只在相邻值发生变化时计数，`30/1.cpp` 要先把棋盘压成稳定状态串再做映射计数，`33/1.cpp` 要同时维护“总出现次数”和“不同归属数”，`35/1.cpp` 则把字符种类统计和重复字符统计拆开处理。

### 区间与前缀和类

`23/1.cpp`、`29/1.cpp`、`31/1.cpp`、`31/2.cpp`、`36/2.cpp` 和 `39/2.cpp` 都依赖累计信息或区间视角。`29/1.cpp` 通过 `max/min` 把矩形裁剪回合法区域，`31/1.cpp` 先把全部平移量累加成总位移，`31/2.cpp` 再进一步把连续操作压成前缀缩放积与前缀旋转和，`36/2.cpp` 把答案拆成前缀最大值和后缀最大值两部分，`39/2.cpp` 则把大量阈值区间叠加转成差分统计。

### 模拟与状态维护类

`1/2.cpp`、`34/1.cpp`、`34/2.cpp`、`36/1.cpp`、`40/3.cpp` 和 `41/3.cpp` 重点在状态更新顺序或矩阵展开方式。窗口置顶、矩阵重排、矩阵转置、网格移动、字符矩阵恢复、循环队列分配都不是单纯套算法模板，更需要先把当前状态、维度变化、更新动作和输出时机梳理清楚。

### 搜索与分层推进类

`36/4.cpp` 和 `38/2.cpp` 都带有“按层扩展”的味道。`36/4.cpp` 用区间层维护当前步数能覆盖的最远范围，`38/2.cpp` 用 BFS 扩展马步状态，两者都要避免重复扫描已经处理过的位置。

### 编码与预处理类

`27/1.cpp`、`28/1.cpp`、`29/2.cpp`、`33/2.cpp`、`38/3.cpp` 和 `40/2.cpp` 都属于“先整理表示或公式，再做计算”的题。`27/1.cpp` 用各位权值的累乘数组还原混合进制每一位，`28/1.cpp` 直接按折现公式累计现值，`29/2.cpp` 通过排序后配合二分检查目标天数是否可行，`33/2.cpp` 则先统一大小写再做集合交并统计。

## 运行方式

建议在仓库根目录编译。

```bash
g++ -std=c++11 programming/test/csp/5/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/27/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/28/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/29/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/29/2.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/30/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/31/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/31/2.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/33/1.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/33/2.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/34/2.cpp -o test && ./test
g++ -std=c++11 programming/test/csp/36/2.cpp -o test && ./test
g++ -std=c++17 programming/test/csp/38/3.cpp -o test && ./test
```

如果当前目录已经在 `programming/test` 下：

```bash
g++ -std=c++11 csp/21/1.cpp -o test && ./test
g++ -std=c++11 csp/41/3.cpp -o test && ./test
```

## 注意事项

- `9/1.cpp` 当前只有空的 `main` 骨架，这次文档仅把它记为待补文件，不当作已完成题解。
- `28/1.cpp` 的关键是每一项现金流都按正确指数折现，浮点输出建议固定格式。
- `29/1.cpp` 计算相交面积时，宽和高都要先用 `max(0, ...)` 裁成非负值。
- `29/2.cpp` 当前写法把任务按时间排序后配合 `upper_bound` 定位超期部分，二分检查时要重点看可行性函数是否单调。
- `30/1.cpp` 把 `8 x 8` 棋盘压成固定长度状态串后再统计，可以避免直接比较二维结构带来的代码噪声。
- `31/1.cpp` 只有平移操作时不需要保存整段历史，直接累计总位移向量即可把单次查询降到 `O(1)`。
- `31/2.cpp` 查询区间时直接用前缀缩放积和前缀角度差恢复变换，避免逐条操作重放。
- `33/1.cpp` 中“出现于多少个集合”与“总共出现多少次”是两套不同统计，不能只用一个计数数组代替。
- `33/2.cpp` 当前实现先统一转小写再做交并集，复盘时重点看“大小写归一 + 去重统计”的处理顺序。
- `34/1.cpp` 这类矩阵重排题先压平再输出通常更稳定，复盘时重点看一维下标如何连续推进。
- `34/2.cpp` 当前转置实现会完整重建一次矩阵，之后还要立刻交换 `n`、`m`，否则查询下标会基于旧维度出错。
- `35/1.cpp` 当前写法在某个字符第三次出现时才标记重复条件成立，复盘时要注意频次判断是在自增前还是自增后触发。
- `36/2.cpp` 这次已补全缺失括号并恢复到可编译状态，后续复盘时重点看前缀和与前后缀最大值如何拆开维护。
- `38/3.cpp` 需要 `C++17` 编译，且恢复摘要字段时依赖之前已经出现过的代码记录。
- `40/2.cpp` 适合固定状态空间、批量查询的题，重点是先做整表预处理而不是逐次重算。
- `41/3.cpp` 同时维护按左端点排序和按长度排序的空闲区间，删除队列时要注意相邻空闲段合并。
